apiVersion: v1
kind: Service
metadata:
  name: defi-copilot-web
  namespace: defi-copilot
  labels:
    app: defi-copilot-web
spec:
  type: ClusterIP
  selector:
    app: defi-copilot-web
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
